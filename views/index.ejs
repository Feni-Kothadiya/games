<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <link rel="manifest" href="/public/manifest.json">
    <link rel="icon" href="/public/images/favicon.ico">

    <% if (category) { %>
        <title><%= category %> Games - Dora Dora Play</title>
    <% } else { %>
        <title>Dora Dora Play - Premium Browser Games</title>
    <% } %>

    <meta property="canonical" content="<%= url %>" />
    <meta rel="canonical" content="<%= url %>" />
    <meta name="keywords" content="free online games, browser games, dora dora play">
    <meta name="description"
        content="Premium instant browser games with no downloads. Discover action, puzzle, arcade, and multiplayer games on Dora Dora Play.">
    <meta property="og:title" content="Dora Dora Play - Premium Browser Games">
    <meta property="og:url" content="https://taptapgames.fun/">
    <meta property="og:description"
        content="Premium instant browser games with no downloads. Play top titles across desktop and mobile.">
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="Dora Dora Play - Premium Browser Games">
    <meta name="twitter:description"
        content="Premium instant browser games with no downloads. Play top titles across desktop and mobile.">
    <meta name="twitter:image" content="https://taptapgames.fun/images/taptap_meta.png">
    <link rel="preload" as="style" href="/public/content/themes/default/style/home.css" />
    <%- include('partials/head') %>
    <link rel="stylesheet" type="text/css" href="/public/content/themes/default/style/home.css" />
</head>

<body id="page-top" class="page-home">
    <span class="search-trans-bg"></span>
    <noscript>
        <iframe src="https://www.googletagmanager.com/ns.html?id=GTM-5BGZ3CVT" height="0" width="0"
            style="display:none;visibility:hidden"></iframe>
    </noscript>

    <div class="app-wrapper">
        <%- include('partials/nav') %>

        <div class="app-shell">
            <%- include('partials/sidebar') %>

            <main class="app-main">
                <section id="home-search-section" class="home-search-inline" aria-label="Search results"
                    style="display: none;">
                    <h2 class="home-section-title">Search Results</h2>
                    <div id="home-search-results" class="home-games-grid"></div>
                    <p id="home-search-empty" class="home-search-empty" style="display: none;">No games found. Try a
                        different keyword.</p>
                </section>

                <div id="home-default-content">
                    <section class="home-categories-rail" aria-label="Category shortcuts">
                        <a href="/" class="home-category-pill <% if (!category) { %>home-category-pill--active<% } %>">All</a>
                        <% categories.forEach(function(item) { %>
                            <a href="/category/<%= item.category %>"
                                class="home-category-pill <% if (category && (category === item.category || category === item.category_name)) { %>home-category-pill--active<% } %>">
                                <%= item.category_name %>
                            </a>
                        <% }); %>
                    </section>

                    <section class="home-games-section">
                        <h2 class="home-section-title">
                            <% if (category) { %><%= category %> Collection<% } else { %>Featured Collection<% } %>
                        </h2>
                        <div class="home-games-grid">
                            <% data.forEach(function(item) { %>
                                <a href="<%= item.pagelink.replace(/ /g,'_') %>" class="home-game-card home-game-card--small">
                                    <span class="home-game-card-overlay"></span>
                                    <span class="home-game-card-media">
                                        <img src="<%= item.thumb %>" alt="<%= item.name %>" width="350" height="350" loading="lazy" decoding="async">
                                    </span>
                                    <span class="home-game-card-title"><%= item.name %></span>
                                </a>
                            <% }); %>
                        </div>
                    </section>
                </div>
            </main>
        </div>
    </div>

    <article class="bottom_info container home-info-card">
        <% if (category) { %>
            <h2><%= category %> Games</h2>
        <% } else { %>
            <h2>About Dora Dora Play</h2>
        <% } %>
        <p>Dora Dora Play is a modern browser gaming destination focused on reliable performance, curated quality, and
            clean user experience across desktop and mobile devices.</p>
        <p>Explore action, arcade, puzzle, driving, multiplayer, and strategy titles without install friction. New game
            drops and category updates are published regularly.</p>
    </article>

    <%- include('partials/footer') %>
</body>

</html>
